# URL del backend (incluí el /api final).
# DESARROLLO: dejalo vacío → usa el proxy de Vite hacia localhost:3001.
# PRODUCCIÓN (Vercel): la URL de tu backend en Render + /api
#   Ej: https://ocr-caruso-backend.onrender.com/api
VITE_API_URL=
